describe('AngularRateLimiterProvider', function() {
    
    xit('throws error on invalid configuration');
    xit('throws error if multiple catch all rules');

    xit('configures interceptor only once');

    xit('creates rules correctly');
});